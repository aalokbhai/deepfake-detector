Django>=6.0,<7.0
whitenoise>=6.7,<7.0
gunicorn>=23.0,<24.0
Pillow>=11.0,<12.0
opencv-python-headless>=4.10,<5.0
numpy>=1.26,<3.0

# CPU-only PyTorch build (much smaller download than the CUDA build, and
# free-tier hosts have no GPU anyway). pip reads this extra index too.
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.9,<2.13
torchvision>=0.20,<0.28
